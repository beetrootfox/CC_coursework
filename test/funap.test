/*
Tests function definition and application.
Function application looks a bit like pipelining:
	({function expression} <- {argument expression})
It is possible to use a function with no arguments.
*/
fun(a;b){
	(!(a == ~c) <- a;b); 
};
main(){
	(fun<-); 
}


